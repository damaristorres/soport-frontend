import { Procesador } from "../../enum/procesador.enum";
import { Tipo } from "../../enum/tipo.enum";

export class Computadora {

    id!: number;
    usuario!: string;
    area!: string;
    tipo!: string;
    modelo!: string;
    procesador!: string;
    
  }
  